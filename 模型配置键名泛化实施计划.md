# 青野 Verda · 模型配置键名泛化实施计划（`zhipu_*` → `llm_*`）

> 模式：**方案先行（Plan）**。本文件为方案定稿，待你确认后我才落地代码。
> 配套技能：`architecture-first-fix`（根因级修复，不做表面补丁）。

---

## 0. 目标与背景

**触发**：「服务商预设」上线后发现——切厂商后配置键仍是 `zhipu_*`（如切到 DeepSeek，
保存的仍是 `zhipu_api_key`/`zhipu_base_url`），功能正确但语义错位，存在「智谱 Key
跟着 DeepSeek 端点一起被提交」的整类误操作风险。

**目标**：把 LLM 连接配置键从厂商前缀 `zhipu_*` 泛化为厂商无关的 `llm_*`，
使键名与「当前连接的厂商」解耦；**不改你的 `.env`、不丢已存配置**。

**已确认范围边界**：
| 项 | 决策 |
|---|---|
| 泛化范围 | 仅 LLM 连接 6 键（key/base_url/model×4） |
| 不改范围 | `bocha_*`（搜索）、`*_cookie`（平台采集）、root/health 的 `llm_configured` 字段名 |
| env 兼容 | 旧名 `ZHIPU_*` 继续可读（`.env` 一行不用改），新名 `LLM_*` 优先 |
| DB 兼容 | settings 表旧键启动时**幂等迁移**到新键并删除旧键 |
| 前端 | 键名同步 + `configured.zhipu` → `configured.llm` |

---

## 1. 问题根因（架构层）

| # | 根因 | 位置 | 后果 |
|---|---|---|---|
| **K1** | 配置键名以厂商编码：`zhipu_*` 写死厂商身份，但连接本身是 **OpenAI 兼容协议**（与厂商无关），键名承载了不该有的语义 | `config.py:21-32` / `runtime_config.py:40-46` | 切厂商后键名与内容错位，状态条/日志/报错误导，误提交风险 |
| **K2** | 前端预设库（`llmProviders.ts`）把「选厂商」落成「写 `zhipu_base_url` 键」 | `SettingsPage.tsx`（选择器 onClick） | 每次切厂商都在错位键名上操作，无厂商身份可核对 |
| **K3** | `configured_flags` 标记 `zhipu` 硬编码 | `runtime_config.py:210` | 配 DeepSeek Key 也显示「zhipu 已配置」，语义假 |

> **结论**：UI 防呆（切厂商时弹确认）只缓解 K2 的症状，K1/K3 的键名债还在，未来每个
> 新厂商/新功能继续踩。根治 = 键名泛化，一次迁移铲掉整类问题。

---

## 2. 现状触点清单（已逐处核实）

### 后端（5 文件 13 处）
| 文件 | 位置 | 内容 |
|---|---|---|
| `core/config.py` | 字段 `zhipu_api_key` / `zhipu_base_url` / `zhipu_model` / `zhipu_model_core` / `zhipu_model_aux` / `zhipu_model_fast`（:21-32）；`llm_configured` property（:64） | env 默认值来源 |
| `core/runtime_config.py` | CONFIG_SCHEMA 6 键（:40-46）；SECRET_KEYS `zhipu_api_key`（:65）；`configured_flags` 标记 `"zhipu"`（:210） | 配置真相源 |
| `core/llm.py` | `_get_client` key/base_url 判断与签名（:58,63-64）；`chat`/`chat_stream` 默认模型（:114,229）；未配置报错文案（:60） | 消费端 |
| `core/orchestrator.py` | `_model(tier)` 三档读取（:100-103） | 消费端 |
| `main.py` | root/health `llm_configured`（:50,59）；`llm_ping` 返回 model（:75） | 消费端 |

### 前端（2 文件，SettingsPage 触点全仓核实为 **11 处**）
| 文件 | 位置 | 内容 |
|---|---|---|
| `types.ts` | `SettingsConfigured { zhipu; bocha }`（:349） | 契约 |
| `pages/SettingsPage.tsx` | FIELD_LABEL 键 6 处（:58-63）；MODEL_FIELDS 4 键（:74-77）；**activePreset memo 读 `form.zhipu_base_url`（:178,180）**；**状态条 `resp.values.zhipu_model`（:231）**；`configured.zhipu` 2 处（:238,244）；选择器写入 `setField('zhipu_base_url',…)`（:306）；**自定义提示 `form.zhipu_base_url`（:346）** | UI 消费 |

> ⚠️ 粗体 4 处为评审补全：漏改会导致服务商 chip 永不命中（activePreset 恒 null）、状态条默认模型恒显示 `-`、自定义提示恒错。

> 注：`lib/api.ts` 无键名依赖（动态 patch），**不用改**；`lib/llmProviders.ts` 只存值不存键，仅 :15 注释提及旧键名，落地时顺带更新注释。

### 独立副本与文档（明确不改，防误判）
| 路径 | 性质 | 处理 |
|---|---|---|
| `api/`（`index.py` + 属性式 `config/llm/orchestrator`） | **Vercel 旧部署副本**：无 runtime_config / 无 `/api/settings` / 无 DB 覆盖层，内部字段↔env 自洽（`zhipu_*`↔`ZHIPU_*`） | **本次不改**。若未来该副本需 `LLM_*` env 再同步字段名（独立任务） |
| `docs/系统升级实施方案.md`、`模型配置模块实施计划.md` | 历史文档 | 不改 |

### 数据现状（降低风险的关键事实）
- 真实库 `backend/app/data/verda.db` 的 `settings` 表 **0 条**（此前测试全在隔离 DB）→ 迁移在真实环境为 no-op。
- `.env` 用旧名 `ZHIPU_API_KEY` 等 → **必须兼容旧 env 名**，否则升级后 Key 失效。

---

## 3. 总体方案

```
                    env 默认(.env)             DB 覆盖(settings 表)
  旧名 ZHIPU_* ──▶┐                        ┌─▶ zhipu_* (迁移前)
                  ▼                        │      │ 启动幂等迁移
  新名 LLM_*  ──▶ Settings(llm_* 字段)    ◀─┘      ▼
                  └──────────▶ llm_* (统一键名) ◀── llm_* (迁移后)
                                   │
               llm.py / orchestrator.py / main.py / /api/settings  ← 全读 llm_*
```

三层同步：**env 读取层**（config.py 双名兼容）→ **真相源**（runtime_config schema 换键 +
DB 幂等迁移）→ **消费端**（llm/orchestrator/main + 前端）。

---

## 4. 详细设计

### 4.1 键名映射

| 旧键（DB / schema / env 字段） | 新键 | 分组 | 说明 |
|---|---|---|---|
| `zhipu_api_key` | `llm_api_key` | provider | 密钥（SECRET_KEYS 同步） |
| `zhipu_base_url` | `llm_base_url` | provider | |
| `zhipu_model` | `llm_model` | model_matrix | 默认模型 |
| `zhipu_model_core` | `llm_model_core` | model_matrix | 核心章 |
| `zhipu_model_aux` | `llm_model_aux` | model_matrix | 辅助章 |
| `zhipu_model_fast` | `llm_model_fast` | model_matrix | 杂务快速 |

### 4.2 env 兼容层（`config.py`）—— 用 default_factory 双源兜底，不用 AliasChoices

每个 `llm_*` 字段：`Field(default_factory=...)`，工厂按「**新名由 pydantic-settings
正常读取（LLM_*）→ 缺失时兜底旧名**」实现：

```python
import os
from dotenv import dotenv_values          # python-dotenv 已在依赖里

_LEGACY_ENV = dotenv_values(_ENV_FILE)    # .env 文件旧名一次读入（模块级）

def _legacy(name: str) -> str:
    """旧名兜底：os.environ 注入优先，其次 .env 文件。"""
    v = os.environ.get(name)
    if v is None:
        v = _LEGACY_ENV.get(name)
    return v or ""

# 字段示例
llm_api_key: str = Field(default_factory=lambda: _legacy("ZHIPU_API_KEY"))
llm_model:    str = Field(default_factory=lambda: _legacy("ZHIPU_MODEL") or "glm-5.1")
```

- **为何不用 `AliasChoices`**：pydantic-settings 的 alias 语义坑多（需 `populate_by_name`
  配合、多候选顺序依赖版本行为）；`default_factory` 仅在 env 未提供新名时触发，
  天然实现「新名优先、旧名兜底」，且能同时覆盖 **os.environ 注入** 与 **.env 文件**两种来源。
- 优先级：新名 `LLM_*`（os 环境 / .env）> 旧名 `ZHIPU_*` > 字段内建默认。
- 落地前先用 10 行脚本验证三场景（只有旧名 / 只有新名 / 双名并存），见 §6-①。

### 4.3 DB 幂等迁移（`db.py` + `runtime_config.py`）

`db.py` 新增两个函数（走既有 `_LOCK` 线程安全层）：
```python
def delete_setting(key: str) -> None: ...
def migrate_settings(mapping: dict) -> None:
    """单事务：旧键→新键（新键已存在则跳过不覆盖），成功后删除旧键。幂等。"""
```

`runtime_config.py` 增加：
```python
LEGACY_KEY_MAP = { "zhipu_api_key": "llm_api_key", ... }   # §4.1 全量映射
_MIGRATED = False
def migrate_legacy_settings() -> None:   # 模块锁保证进程内一次
    db.migrate_settings(LEGACY_KEY_MAP); _MIGRATED = True
```

**触发点（评审 P1-② 修订）**：`main.py` 加 **lifespan** 启动时显式调用
`migrate_legacy_settings()`，**fail-fast**（迁移失败即启动失败，显式暴露，不静默丢覆盖值）；
`_build_effective()` 保持纯读，不埋写操作。迁移函数进程内幂等（模块锁 + `_MIGRATED` 标记），
无旧键时单次空扫描即返回。

### 4.4 消费端换键（纯改名，无逻辑变化）

- `llm.py`：`:58,63-64` → `llm_api_key`/`llm_base_url`；`:114,229` → `llm_model`；
  未配置文案改为「未配置 LLM API Key（backend/.env 的 LLM_API_KEY 或「模型配置」页，
  旧名 ZHIPU_API_KEY 仍兼容）」。
- `orchestrator.py` `:100-103` → `llm_model_core`/`llm_model_fast`/`llm_model_aux`。
- `main.py` `:50,59` → `llm_api_key`；`:75` → `llm_model`。
- `runtime_config.py`：CONFIG_SCHEMA/SECRET_KEYS 换键；`configured_flags` 返回
  `{"llm": bool(eff.get("llm_api_key")), "bocha": ...}`。

### 4.5 前端同步（2 文件 + 1 注释）

- `types.ts`：`SettingsConfigured { llm: boolean; bocha: boolean }`。
- `SettingsPage.tsx`（**全 11 处**，评审 P1-① 补全）：
  - FIELD_LABEL 键名 6 处换 `llm_*`（显示文案不变：Base URL / API Key / 默认模型…）；
  - MODEL_FIELDS 换 `llm_model*` 4 键；
  - **activePreset memo：`form.zhipu_base_url` → `form.llm_base_url`（:178,180）**；
  - **状态条默认模型：`resp.values.zhipu_model` → `resp.values.llm_model`（:231）**；
  - 状态条 `resp.configured.zhipu` → `resp.configured.llm`（2 处 :238,244）；
  - 选择器 onClick：`setField('llm_base_url', p.baseUrl)`（:306）；
  - **自定义提示：`form.zhipu_base_url` → `form.llm_base_url`（:346）**。
- `lib/llmProviders.ts`：仅更新 :15 注释（键名已泛化为 `llm_*`），无代码改动。

### 4.6 `.env.example` 与 `.env`

- `.env.example`：键名更新为 `LLM_API_KEY`/`LLM_MODEL`/`LLM_BASE_URL`，注释注明
  「旧名 `ZHIPU_*` 仍兼容」。
- 你的 `.env`：**不改**（4.2 兼容层自动兜底），可选后续手动改名。

---

## 5. 风险与缓解

| # | 风险 | 等级 | 缓解 |
|---|---|---|---|
| R1 | 旧前端页面（未刷新）拿 `zhipu_*` 键保存 → 不生效 | 低 | `apply_settings` 本就忽略未知键、不报错；重启后端后**先刷新浏览器再操作**（升级固有动作） |
| R2 | env 兼容层行为不符（读取顺序/双名） | 低 | 先跑 §6-① 独立验证脚本再动 schema；default_factory 语义直白，风险可控 |
| R3 | DB 迁移覆盖用户手动值 | 低 | `migrate_settings` 新键已存在则**跳过不覆盖**（保留用户新配置）；真实库 0 条，no-op |
| R4 | 迁移函数并发重入 | 低 | 模块锁 + `_MIGRATED` 标记，进程内仅执行一次 |
| R5 | 漏改触点导致运行时 KeyError/读不到 | 低 | §2 触点清单已全列；改完跑隔离 DB 全链路自测（§6）兜底 |

---

## 6. 验证计划（隔离临时 DB :8011，不碰真实库/`.env`）

1. **env 兼容脚本**：临时 .env 三场景（仅 `ZHIPU_*` / 仅 `LLM_*` / 双名并存）
   → 实例化 Settings 断言取值正确且「新名优先」。
2. **迁移自测**：临时 DB 预置 `zhipu_*` 旧记录（含假 key）→ 起后端 → 断言
   GET `/api/settings` 出现 `llm_*` 且值迁移保留、`zhipu_*` 消失；DB 内旧键已删。
3. **PUT 回归**：改 `llm_base_url`/`llm_model_core` 生效；密钥空串保留；非法类型 422。
4. **configured**：`{"llm": …, "bocha": …}`（llm 为真 ⇔ 有 key）。
5. **llm_ping**：空 key 临时 env 下返回 `not_configured` + 新文案。
6. **接口回归**：`/`、`/health`、`/api/experts`（48）、`/api/dashboard`。
7. **前端**：`npx tsc -b --noEmit` 通过；grep 确认无 `zhipu_` 残留（前端 src）。
8. 清理测试实例与临时 DB/临时 env。

---

## 7. 落地顺序（分阶段，每阶段独立验证）

| 阶段 | 内容 | 关卡 |
|---|---|---|
| S1 | `config.py` env 兼容层 + §6-① 验证脚本 | 脚本三场景通过 |
| S2 | `db.py` 迁移函数 + `runtime_config.py` schema/迁移/configured | import 无错 |
| S3 | 消费端换键（llm/orchestrator/main）+ `main.py` 挂 lifespan 显式迁移（fail-fast） | 隔离 DB 起后端，§6-②~⑥ 全过 |
| S4 | 前端 `types.ts` + `SettingsPage.tsx` + `.env.example` | tsc 通过 + 无残留 grep |
| S5 | 你 `./restart.sh` → 浏览器验证（§6 手测清单） | 状态条/保存/切厂商正常 |

---

## 8. 回滚

- 全部改动**未 commit**；任一步失败 `git checkout -- <改动文件>` 即整体还原。
- 迁移幂等且真实库 0 条 → 无数据回滚负担；重复执行安全。

---

## 9. 已确认与边界

1. 键名定为 `llm_*`（`llm_api_key` / `llm_base_url` / `llm_model*`）；评审 4 项已并入（P1-①②、P2-③④）。
2. **边界明示（评审 P2-④）**：本次泛化是未来「多厂商并行 / 多连接配置」的**必要非充分**步骤——
   现架构仍是**单连接**（一套 `llm_*` 键，切厂商 = 改值）。多连接需把单连接升级为连接列表，
   属独立后续任务，不在本计划范围。
3. 落地按 §7 S1→S5 推进，最后你 `./restart.sh` 后浏览器验证。

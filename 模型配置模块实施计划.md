# 青野 Verda · 前端「模型配置」模块 实施计划

> 模式：**计划模式（Plan）**。本文件为方案定稿，待你确认后我才落地代码。
> 配套技能：`architecture-first-fix`（根因级修复，不做表面补丁）。

---

## 0. 目标与已确认决策

**目标**：在前端增加「模型配置」模块，让用户在 UI 上自助配置 LLM 提供方 / 模型 / 参数，
不必再手改 `backend/.env`（用户原话："在 env 文件里改代码"）。

**已与你确认的决策**：

| 项 | 决策 |
|---|---|
| 运行时配置存储 | **SQLite `settings` 表**（复用现有 `db.py` 线程安全层） |
| 安全策略 | **服务端存密 + GET 脱敏 + 无鉴权**（localhost 单用户，实现最简） |
| 配置范围 | **LLM 提供方 Key + Base URL + 模型矩阵 + 推理参数 + 博查搜索 Key 全部进模块** |

---

## 1. 问题根因（架构层，非表面症状）

"没有设置页"只是表面症状。真正根因在**配置子系统的设计层**，共 4 条（按 `architecture-first-fix` 五问法上溯）：

| # | 根因 | 位置 | 后果 |
|---|---|---|---|
| **RC1** | 配置只读冻结：`get_settings()` 是 `@lru_cache` 单例，在 `main.py:24` 模块导入时一次性从 env 加载，全程不再读 | `config.py:64` / `main.py:24` | 没有"运行时覆盖"通道 |
| **RC2** | LLM 客户端单例缓存：`llm.py:26` 的 `_client` 首次用启动时的 key 建好就再不换 | `llm.py:26,40-54` | 即便加了覆盖配置，不重建客户端也**完全不生效** |
| **RC3** | 缺持久化/覆盖层：没有 DB 表或文件存"用户运行时改过的配置" | `db.py` | 配置只活在 env |
| **RC4** | 无鉴权 + 密钥暴露风险：全接口 `CORS *`、无 auth | `main.py:28-34` | 朴素地加设置接口把 Key 明文回传 = 把密钥暴露给任何能访问端口的人 |

> **关键结论**：RC1/RC2 解释了为什么"写回 .env"式做法救不了你——运行中的进程读的是冻结单例和缓存客户端，必须重启才生效。正确做法是**从架构上补一层运行时可覆盖配置 + 客户端失效机制**，前端表单只是这层之上的入口。

---

## 2. 同类项目调研结论（已搜索）

| 项目 | 核心机制 | 可借鉴点 | 参考 |
|---|---|---|---|
| **Open WebUI** | 协议导向的"连接"：Base URL + API Key，模型 ID 白名单（手动或 `/models` 自动发现），连接可启用/停用，可设默认模型 | 提供方连接抽象 + **连接校验** + 启用/停用 | docs.openwebui.com |
| **Dify** | 工作空间"模型供应商"卡片：凭据**加密存储**(PKCS1_OAEP)、**按角色设默认模型**(系统推理/Embedding/Rerank/STT/TTS)、启用/停用、使用前校验凭据 | **按角色设默认模型** + 凭据加密 + 后端定义模型(前端无关) | docs.dify.ai / deepwiki |
| **LibreChat** | YAML 定义自定义 endpoint + 用户**预设(Preset)**：每个 endpoint 可配 `paramDefinitions`(temperature 滑块范围/默认)、用户存多个预设随时切换 | **按模型 granular 参数**(temp/max_tokens/timeout) + 预设可复用 | librechat.ai/docs |

**映射到本项目**：本项目已有"模型矩阵"雏形——`config.py` 的 `zhipu_model`(默认)/`zhipu_model_core`(核心章)/`zhipu_model_aux`(辅助章)/`zhipu_model_fast`(杂务)，`orchestrator.py` 按章节调度不同模型。这**正好对应 Dify 的"按角色设默认模型"**。
→ 最贴合的借鉴：**Dify 的角色默认 + Open WebUI 的连接校验 + LibreChat 的参数细调**。

---

## 3. 总体方案

在**不破坏现有调用契约**的前提下，新增一层"运行时配置覆盖"：

```
请求时读取
  ┌─────────────┐   env 默认    ┌──────────────────────┐
  │ get_settings│──────────────▶│                      │
  └─────────────┘               │  runtime_config       │
                                │  .get_effective_settings()  ← 合并
  ┌─────────────┐   DB 覆盖     │                      │
  │  settings   │──────────────▶│  (env + 运行时覆盖)    │
  │  (SQLite)   │               └──────────┬───────────┘
  └─────────────┘                          │ 供以下模块在请求时读取
                                            ├─ llm.py      （重建客户端 + 默认模型）
                                            ├─ search.py   （博查 Key/Base/超时）
                                            ├─ orchestrator.py（模型矩阵 core/aux/fast）
                                            └─ main.py     （health/ping/已配置状态）

  PUT /api/settings ──▶ apply_settings() 落库 + 失效 LLM 客户端缓存
```

**要点**：
- env 仍是默认值来源；DB 只存"用户改过的键"（空值回落 env 默认）。
- `_client` 改为**按签名 `(key, base_url, timeout, retries)` 重建缓存**，配置变更即时生效，无需重启（根治 RC2）。
- 密钥只在服务端；`GET` 永远脱敏。

---

## 4. 后端改动清单（文件级，最小侵入）

| 文件 | 改动 |
|---|---|
| `backend/app/core/db.py` | `_init_schema` 加 `settings(key,value)` 表；新增 `get_setting/set_setting/get_all_settings`（复用 `_LOCK`/`_connect`，线程安全） |
| `backend/app/core/runtime_config.py` *(新建)* | `CONFIG_SCHEMA`（**每键强制带 `default/type/group`**，单点定义可配键）；`SECRET_KEYS`、`GROUP_FIELDS`；**可失效内存缓存** `_EFF_CACHE`+`_EFF_VERSION`：`get_effective_settings()` 走缓存，仅 `_EFF_VERSION` 变化时重算合成 env+DB（避免每次 `chat()` 读 DB，且修复 RC1 同类"读策略"错误）；`apply_settings(patch)`（**类型转换失败整包 `raise HTTPException(422, {field: msg})`**+落库+忽略未知键+末行 `_EFF_VERSION += 1` 失效缓存）、`mask_secret()/mask_effective()`（脱敏） |
| `backend/app/core/llm.py` | `_client` 改为签名重建（新增 `_client_sig`、`invalidate_client()`）；`chat()/chat_stream()` 读运行时默认模型 |
| `backend/app/core/search.py` | `search()` 读运行时 `bocha_api_key/base_url/search_timeout` |
| `backend/app/core/orchestrator.py` | **删除/弃用模块级 `_settings = get_settings()`（orchestrator.py:44）**；`_model(tier)` 内联读 `get_effective_settings()` 返回运行时模型矩阵（core/aux/fast），确保 RC1 对模型矩阵全收口、无遗漏（grep 确认 `_settings` 仅用于 `_model` 三处） |
| `backend/app/main.py` | **删除模块级 `settings = get_settings()`（main.py:24）**，`root()/health()/llm_ping()` 端点内联调用 `get_effective_settings()`（避免 env 冻结单例导致 `llm_configured`/`model` 回显陈旧）；新增 `GET /api/settings`（脱敏返回+是否已配置）、`PUT /api/settings`（校验→落库→失效客户端+失效配置缓存→可选 ping） |

> **调用方零改动**：`chat()` / `search()` / `multi_search()` 对外签名不变；`orchestrator._model()` 仅内部实现切换。无循环依赖、无新增上帝对象。

---

## 5. 前端改动清单

| 文件 | 改动 |
|---|---|
| `frontend/src/types.ts` | 新增 `SettingsValues` / `SettingsResp` 类型 |
| `frontend/src/lib/api.ts` | 新增 `fetchSettings()` / `saveSettings(patch)` |
| `frontend/src/pages/SettingsPage.tsx` *(新建)* | 分组表单（沿用现有 `VCard`/`VButton`）：① 提供方连接（Base URL + API Key 密码框 + **「连接测试」按钮**调 `/api/llm/ping`）② 模型矩阵（默认/核心章/辅助章/杂务快速 四角色下拉，支持手动输入模型 ID）③ 推理参数（temperature/max_tokens/timeout/retries）④ 搜索提供方（博查 Key + Base URL）。**保存即生效**，顶部显示"当前模型/已配置" |
| `frontend/src/App.tsx` | 在 `AppLayout` 路由组加 `<Route path="/settings" element={<SettingsPage />} />` |
| `frontend/src/layout/VSidebar.tsx` | 顶部 lucide 导入补 `Settings`（当前未导入），导航加 `{ to: '/settings', label: '模型配置', icon: Settings }` |

**前端表单行为**：
- 加载时 `GET /api/settings` 填充；**密钥字段显示为脱敏值、输入框留空**（防泄露）。
- 保存时：非密钥字段全量发送；密钥字段**仅当用户实际输入了内容才发送**，空值=保留原值（后端 `apply_settings` 据此跳过）。
- 保存成功 → 后端失效 LLM 客户端 → 下次调研即用新配置，**无需重启**。

---

## 6. 接口数据契约

**`GET /api/settings`** 响应：
```json
{
  "values": {
    "zhipu_api_key": "sk-****abcd",
    "zhipu_base_url": "https://open.bigmodel.cn/api/paas/v4",
    "zhipu_model": "glm-5.1",
    "zhipu_model_core": "glm-5.2",
    "zhipu_model_aux": "glm-5.1",
    "zhipu_model_fast": "glm-z1-air",
    "llm_timeout": 180,
    "llm_max_retries": 1,
    "bocha_api_key": "sk-****",
    "bocha_base_url": "https://api.bocha.cn/v1",
    "search_timeout": 30,
    "enable_demo_fallback": true
  },
  "secrets": ["zhipu_api_key", "bocha_api_key", "douyin_cookie", "xhs_cookie", "bilibili_cookie"],
  "groups": { "provider": [...], "model_matrix": [...], "params": [...], "search": [...], "platform": [...] },
  "configured": { "zhipu": true, "bocha": true }
}
```

**`PUT /api/settings`** 请求体：`{ "patch": { "zhipu_model_core": "glm-5.2", "zhipu_api_key": "" } }`
（密钥为空字符串 → 后端保留原值；未知键被忽略；类型按 `CONFIG_SCHEMA` 转换）

---

## 7. 安全设计（RC4 闭环）

- 密钥**只存后端 SQLite**，绝不出现在前端 JS 或 GET 明文。
- `GET` 始终返回脱敏值（`sk-****abcd`）；`PUT` 才接收明文，空值=保留。
- 当前无鉴权（符合已确认的"无鉴权 localhost 单用户"决策）。
- **绑定与暴露面（P1）**：`config.py:54` 的 `app_host` 默认 `0.0.0.0` 会监听所有网卡；在「无鉴权 + `0.0.0.0`」下，同 LAN 任意主机可 `PUT /api/settings` 覆盖密钥、`GET` 还能拿到脱敏后缀。本期**默认改为 `127.0.0.1`**（仅本机），并在 `.env.example` 注释声明「勿暴露到 `0.0.0.0`/公网」；确需远程时由用户显式改 `APP_HOST` 并自行加防火墙/口令门禁。
- 脱敏值仅遮蔽中段、保留末 4 位（`sk-****abcd`）便于用户核对，属已知轻微泄露（LLM key 危害低），记录为约束。
- **未来可选**：若部署到非本机/公网，再加 `ADMIN_PASSWORD` 口令门禁（不在本期范围）。

---

## 8. 验证方案（自测）

1. **读取脱敏**：`curl localhost:8010/api/settings` → `zhipu_api_key` 为 `sk-****` 形式。
2. **改 Key 生效**：`PUT` 一个新 `zhipu_api_key` → 再 `GET` 仍脱敏 → `curl /api/llm/ping` 用新 Key 返回 `ok:true`。
3. **改模型矩阵生效**：在 UI 把"核心章"改成某模型 → 跑一次 quick 调研 → Trace 里该角色 `model` 字段显示新模型（验证 RC2 客户端重建 + RC1 运行时覆盖）。
4. **持久化**：重启后端 → `GET /api/settings` 仍返回用户配置（DB 落盘，根治 RC3）。
5. **回归**：`chat()` 签名不变，现有调研流水线、SSE 思维流、报告生成不受影响（跑一次 quick 调研确认全链路）。

---

## 9. 影响与风险

- ✅ 过三条硬标尺：根因层级（修到 RC1/RC2/RC3）、非遮盖（真生效）、不引入耦合+提升（新增 cohesive 模块 `runtime_config`，复用 `db` 层，无循环依赖）。
- ✅ 调用方零改动，回归面小。
- ⚠️ 唯一新增范式：配置"env 默认 + DB 覆盖"双层，需在文档/注释中讲清优先级（DB 覆盖 > env）。
- ✅ 评审收敛：4 项 P1（配置缓存 / main.py 单例闭环 / 校验整包 422 / 绑定 127.0.0.1）已并入第 4、7 节，详见第 10 节。

---

## 10. 评审收敛项（tech-solution-review 输出，落地前必做）

基于 `tech-solution-review` 三维评审（生长性 / 健壮性 / 全局兼容性），已将 4 项 P1 直接并入第 4、7 节实现细节；下列为可追溯清单。

**P1（已并入实现细节，必须落地）**

| # | 项 | 落点 |
|---|---|---|
| ① | 有效配置加可失效内存缓存（避免每次 `chat()` 读 DB，且修复 RC1 同类"读策略"错误） | §4 `runtime_config.py`：`_EFF_CACHE`+`_EFF_VERSION`，`apply_settings` 末行失效 |
| ② | `main.py` 模块级 `settings` 单例闭环（防 `llm_configured`/`model` 回显陈旧） | §4 `main.py`：删行 24 绑定，`root/health/llm_ping` 内联读 runtime |
| ②b | `orchestrator.py:44` 模块级 `_settings` 绑定弃用 | §4 `orchestrator.py`：`_model` 内联读 runtime |
| ③ | `apply_settings` 校验失败整包 `422` + 字段级 message | §4 `runtime_config.py` |
| ④ | 安全暴露面：默认绑定 `127.0.0.1` + 文档声明勿暴露公网 | §7 + `.env.example` 注释 |

**P2（建议增强，不阻断）**

- 双源真相单点锁：`CONFIG_SCHEMA` 强制 `default/type/group`，单测锁优先级。
- 配置变更统一广播钩子 `register_on_config_change`（未来多缓存自动失效）。
- Vercel `/tmp` 持久化约束标注（仅本地持久，Vercel 不持久）。
- `VSidebar.tsx` 补 `import { Settings } from 'lucide-react'`（§5 落地清单）。
- `runtime_config` 单测（合并/脱敏/校验/apply 失效）替代纯手工 curl 回归。

## 11. 本期不做（可选后续）

- 口令门禁（仅当部署到非本机时需要）。
- 多套"预设(Preset)"配置切换（LibreChat 式），本期只做单份运行时配置。
- 多提供方（除智谱外接 OpenAI 兼容网关）——本期 Base URL 已可改，但 UI 不预设多连接管理。

---

### ✅ 请你确认
1. 上述方案与文件级改动是否认可？
2. 配置范围/安全策略是否仍按第 0 节已确认项执行？
3. 有无需要补充进本计划的项（如额外要暴露的字段、要不要"连接测试"按钮）？

确认后我按第 4、5 节落地并跑第 8 节自测。

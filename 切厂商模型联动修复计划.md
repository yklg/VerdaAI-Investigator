# SettingsPage · 切厂商模型联动修复

> 模式：**Plan**。本文件为方案定稿，待你确认后我才落地代码。
> 配套技能：`architecture-first-fix`（根因级修复，不做表面补丁）。

---

## 0. 症状与根因

**现象**（用户截图）：选了 DeepSeek 后点「连接测试」报 400：

```
The supported API model names are deepseek-v4-pro, deepseek-v4-flash,
and deepseek-v4-flash-vision-exp, but you passed glm-5-turbo
```

即 base_url 已是 deepseek，但模型字段保持 `glm-5-turbo`（来自 `.env` 的 `ZHIPU_MODEL` 兜底）。

### 根因（按 `architecture-first-fix` 五问法）

| # | 根因 | 位置 | 后果 |
| --- | --- | --- | --- |
| **K1** | chip onClick **只写 `llm_base_url`**，模型 4 字段保持不变 | `SettingsPage.tsx:306` | 切厂商后端点与模型脱钩，必然产生 400 |
| **K2** | 文案「模型建议已随厂商联动」承诺联动行为，实际只联动 datalist **候选**；保存的值不会自动改 | `SettingsPage.tsx:324` vs onClick | 文案与行为错位，用户被误导 |
| **K3** | 缺乏"模型与端点错配"的运行时检测 | SettingsPage 全局 | 用户已存在的错位状态无任何提示，需主动重操作才能恢复 |


> **关键判定**：K1 是真根因（数据写入层职责缺口），修 onClick 即治根；K2 是文档与行为同步；K3 兜底修复用户当前已错位状态。

---

## 1. 修复方案（架构级，推荐）

### 1.1 K1 核心：扩展 chip onClick 联动模型 4 字段

抽 `applyProviderPreset(preset)` 函数（在 `SettingsPage.tsx` 内联定义）取代当前 `setField('llm_base_url', p.baseUrl)`，**总是按映射规则覆盖模型 4 字段** + 通过 setMsg 提示「已同步」。

**模型 4 字段映射规则**（基于厂商 `models: string[]` 数组）：

| 厂商 `models.length` | `llm_model`（默认） | `llm_model_core`（核心章/重活） | `llm_model_aux`（辅助章） | `llm_model_fast`（杂务/极速） |
| --- | --- | --- | --- | --- |
| **0**（如 volces，models=[]+note） | `''`（清空） | `''` | `''` | `''` |
| **1** | `M[0]` | `M[0]` | `M[0]` | `M[0]` |
| **≥ 2** | `M[0]` | `M[0]` | `M[1]` | `M[M.length-1]` |


**理由**：默认/核心章用厂商主推模型（M[0]，通常最强）→ 符合「重活用好模型」既定设计；辅助章用第二档（M[1]）；杂务用末尾（业界惯例：末尾通常是 fast/flash/air 轻量档）。volces 因「模型 ID 是推理接入点 ep-xxx」无法猜，清空并交给用户手填（note 已说明）。

### 1.2 K2 文案修订

- 旧（:324）：`已匹配 {name} 官方端点，模型建议已随厂商联动`
- 新：`已匹配 {name} 官方端点，模型矩阵已自动同步（仍可手改任意 ID）`

> 文案从「建议联动」（模糊）改为「已自动同步」（确定），与行为一致；保留「仍可手改」强调 input/datalist 始终可覆盖。

### 1.3 K3 兜底：加载时错位检测 + 一键同步

**目的**：用户当前已存在的错位（base_url=deepseek + model=glm-5-turbo）无需手动重操作即可恢复；同时覆盖未来「手改 base_url 但忘改 model」场景。

**逻辑**（SettingsPage 渲染期间用 useMemo 派生 `modelMismatch: boolean`）：

```ts
const modelMismatch = useMemo(() => {
  if (!activePreset || activePreset.models.length === 0) return false
  return !activePreset.models.includes(form.llm_model ?? '')
}, [activePreset, form.llm_model])
```

**UI**：在 model_matrix 卡片头部显示一条非阻塞提示条（不阻断默认交互）：

- 文本：`当前默认模型 "{form.llm_model}" 与 {activePreset.name} 端点不匹配，点「连接测试」会报 400。`
- 操作按钮：「一键同步为 {activePreset.models[0]}」 → 调用 `applyProviderPreset(activePreset)`
- 仅当 modelMismatch=true 显示；点击后提示消失（model 已同步）。

### 1.4 关于「用户手填合理模型被误覆盖」风险

切厂商 onClick 总是覆盖 4 字段 → 如果用户已在模型字段填了新厂商的合法模型 ID（罕见但可能），切到同厂商时会再次覆盖为 `models[0]`，可能丢失用户的精细选择。
**缓解**（不实施，理由）：

- 切厂商语义本身就是「切换到新厂商的推荐配置」，用户期望全套更新；
- 用户若有精细意图，可点 chip 后再手改覆盖（成本极低）；
- 加「仅当旧模型属于旧厂商才覆盖」会让代码复杂度显著上升，得不偿失。
- 已用 setMsg 提示「已同步，可手改」告知用户可调整。

---

## 2. 涉及文件

| 文件 | 改动 |
| --- | --- |
| `frontend/src/pages/SettingsPage.tsx` | 新增 `applyProviderPreset(preset)` 函数；扩展 chip onClick 调用它；新增 `modelMismatch` useMemo；model_matrix 卡片头部加条件提示条；文案修订 |
| `frontend/src/lib/llmProviders.ts` | 仅顶部注释追加：onClick 联动映射规则说明（无代码改动） |


**不改**：后端（任何文件）、`api.ts`、`types.ts`、其他页面、applyProvider 不存在（保持现状）、datalist 单例结构、模型 4 字段 schema。

---

## 3. 验证计划

### 3.1 自动化

- `npx tsc -b --noEmit` 通过
- grep 残留：无 `zhipu_*`（沿用上轮已验证零残留）

### 3.2 手动测试矩阵（5 场景）

| # | 操作 | 期望 4 字段结果 |
| --- | --- | --- |
| 1 | 切智谱（bigmodel） | llm_model=glm-5.2、core=glm-5.2、aux=glm-5.1、fast=glm-z1-air |
| 2 | 切 DeepSeek | llm_model=deepseek-chat、core=deepseek-chat、aux=deepseek-reasoner、fast=deepseek-reasoner（M.length=2, M[length-1]=reasoner） |
| 3 | 切 volces | 4 字段全清空，placeholder/note 引导填 ep-xxx |
| 4 | 自定义 URL（手填 base_url 不匹配任何预设） | 4 字段不动，无同步提示条 |
| 5 | 已错位状态进入页面（base_url=deepseek + model=glm-5-turbo） | 显示「一键同步为 deepseek-chat」提示条；点后 4 字段同步 |


### 3.3 不需要

- 后端测试（零改动）
- pytest / 新单测（项目无前端测试基础设施）

---

## 4. 落地阶段（小改动，三阶段即可）

| 阶段 | 内容 | 关卡 |
| --- | --- | --- |
| **S1** | `applyProviderPreset` 函数 + chip onClick 扩展 + 映射规则 | tsc 通过 |
| **S2** | 文案修订 + `modelMismatch` useMemo + 错位提示条 UI | tsc 通过 + 5 场景手动验证 |
| **S3** | `llmProviders.ts` 注释追加 + 残留 grep | 无残留 |


---

## 5. 风险与回滚

| # | 风险 | 缓解 |
| --- | --- | --- |
| R1 | 切厂商误覆盖用户手填的精细模型 | setMsg 提示「已同步，可手改」；用户随时可手输入覆盖 |
| R2 | volces 清空 4 字段用户困惑 | note 文案已说明「模型 ID 填 ep-xxx 推理接入点」+ placeholder |
| R3 | 文案修订后用户误以为不可手改 | 保留「仍可手改任意 ID」字样 |


**回滚**：单文件改动未提交 → `git checkout -- frontend/src/pages/SettingsPage.tsx frontend/src/lib/llmProviders.ts`。零数据影响。

---

## 6. 待你确认

1. **映射规则**（§1.1 表）是否符合你的预期？特别是 `core = M[0]`（最强模型既给默认也给核心章）和 `fast = M[M.length-1]`（末尾最便宜）。如有偏好（如希望 core 用末尾、fast 用首个），可调。
2. **兜底提示条**（§1.3）位置在 model_matrix 卡片头部还是页面顶部状态条？两种方案都能工作，卡片头部更聚焦于模型问题。
3. 确认后按 §4 三阶段落地。
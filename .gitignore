# ============== 依赖与构建产物 ==============
# Node / npm
node_modules/
frontend/node_modules/

# 前端构建输出
dist/
frontend/dist/
frontend/.next/
frontend/.nuxt/
frontend/.cache/

# Python / venv
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
backend/.venv/
*.egg-info/
.eggs/

# TypeScript 构建
*.tsbuildinfo

# ============== 环境变量与密钥（禁止提交） ==============
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
.env.staging
backend/.env
api/.env

# 密钥 / 凭据相关文件
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
id_rsa
id_dsa
id_ed25519
*.pub

# 数据库文件
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal
api/app/data/verda.db
backend/app/data/verda.db

# ============== 本地缓存 / 日志 / 临时文件 ==============
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.local

# 运行时日志目录（restart.sh 输出）
.run-logs/

# 本机 Node 路径回退文件（restart.sh 用，含本机绝对路径，禁止提交）
.node-path

# AI 工具内部产物目录（含本机绝对路径，正式设计文档已迁移到 docs/）
._agent/
.trae/

# 编辑器 / IDE
.idea/
.vscode/
*.swp
*.swo
*.sublime-workspace
.project
.settings/
.classpath

# 采集存储 / 运行时产物（可能含敏感信息）
backend/storage/
frontend/public/charts/
storage/
tmp/
temp/
.cache/

# ============== 云服务 / 平台特定文件 ==============
.vercel
.now
.netlify/
.firebase/
.aws/
.gcp/

# ============== 测试 / 覆盖率 ==============
coverage/
.nyc_output/
*.lcov

# ============== 锁文件（可选） ==============
# package-lock.json 通常提交；如有安全顾虑可在此取消注释
# package-lock.json
# yarn.lock
